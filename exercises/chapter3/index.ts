import { notImplemented } from "../common";
import { pipe } from "fp-ts/lib/function";
import * as O from "fp-ts/Option";

export const isValidName = (name: string): O.Option<string> => notImplemented();

export const greetUser = (name: string): string => notImplemented();
